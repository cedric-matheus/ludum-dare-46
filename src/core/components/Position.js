/**
 * Componente que representa uma posição 2D
 * @property {number} x=0 - Posição no eixo X
 * @property {number} y=0 - Posição no eixo Y
 */
const Position = { x: 0, y: 0 };

export default Position;
