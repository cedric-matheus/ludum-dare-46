import Position from './Position';

/**
 * Componente que representa um personagem
 * @property {Position} position - Posição do personagem
 */
const Character = { position: { ...Position } };

export default Character;
